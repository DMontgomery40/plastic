# Sleep experiment outputs, 2026-09-23 (compact, reproducible identities)

Each folder is one `scripts/experiments/sleep_controls.py` run: `sleep_controls.md` (table),
`sleep_controls.json` (all arms, per-probe replies), and `sleep_<arm>_report.json` (the sleep run's
own report: harvest, before/after measurements, gate, dreams where applicable). No weights.
Runs that still had a local experiment store also carry `sessions/{teach,rolled}/` (the source
sessions' transactions, traces and metadata). The same archive is browsable in the playground's
Sleep tab; [the observatory page](../../sleep-observatory.md) describes that view and the
exporter (`scripts/export_sleep_observatory.py`) that turns these folders into its data.

| Run | Checkpoint | Digest (prefix) | Code (see note) | Question |
| --- | --- | --- | --- | --- |
| `base_dryrun_*_report.json` | TTT-MLP-760M base (RetentionLabs conversion, public) | see report | ≈ 3992890 (unrecorded) | mechanics only |
| `sleep_controls_step50` | SFT step 50 of 250 (intermediate, not published) | 229e97b67875b732 | ≈ 6334247 (unrecorded) | null baseline, W0 × 10 steps |
| `sleep_controls_step100_all40` | SFT step 100 (intermediate) | 5d7f0b9c6c1a8d4f | ≈ 93d524d (unrecorded) | all parameters × 40 steps: collapse passed the NLL gate |
| `sleep_controls_step100_w0_40` | step 100 | 5d7f0b9c6c1a8d4f | ≈ a11157b (unrecorded) | W0 × 40, assistant-only labels: collapse |
| `sleep_controls_step100_w0_40_all` | step 100 | 5d7f0b9c6c1a8d4f | ≈ 9a1b502 (unrecorded) | W0 × 40, user tokens supervised |
| `sweep_step100_*` | step 100 | 5d7f0b9c6c1a8d4f | ≈ c564390 (unrecorded) | regime sweep, replay on raw turns |
| `study_step100_w0` | step 100 | 5d7f0b9c6c1a8d4f | ≈ 633a899 (unrecorded) | templated study set, prompt-loss weight 0.2 |
| `study_step100_w0_plw1` | step 100 | 5d7f0b9c6c1a8d4f | ≈ 9a1b502…c564390 (unrecorded) | templated study set, prompt-loss weight 1.0 |
| `dream_step100_w0` | step 100 | 5d7f0b9c6c1a8d4f | ≈ 3828ac6 (unrecorded, pre ASTRA-175 fixes) | first dream run, free-form prompts |
| `dream_step100_w0_conditioned` | step 100 | 5d7f0b9c6c1a8d4f | `08e726a+dirty` as recorded; the uncommitted patch in the working tree at launch was not captured, so the exact source is not identified beyond 08e726a | fact-conditioned dreams (the teacher is shown the accepted turn): 48 generated, 47 duplicates of one greeting, 1 kept with gain +2.21 over the reset model and a no-quote session-state score difference of −0.60 (the positive gap disappears without the quoted turn; not an isolated fast-weight intervention); gate passed; taught recall 0/6 before and after, the one new verbatim hit is the general-knowledge probe (capital of France), cluster share 0.03, held-out NLL 1.683 → 1.574 |
| `surprise_vs_entropy_step100` | step 100 | 5d7f0b9c6c1a8d4f (the saved JSON's `checkpoint_digest` field, 55cce938…, is an ad-hoc hash over the json files and weights from the script's first version; later runs record the backend digest) | 489d7ee (recorded `489d7ee+dirty`; the dirty files were another agent's uncommitted docs, no source) | build-order step 4: does inner surprise predict next-token error or loops beyond output entropy? 60 held-out SmolTalk test rows (revision 5feaf2fd), 7,367 assistant tokens; free generation on 16 prompts at T 0.3 and 0.7. Entropy alone: Spearman 0.85 with next-token NLL, AUC 0.85 for the top-1 miss. Adding surprise: +0.002 R², +0.004 AUC (bootstrap over conversations, both intervals above zero but tiny); every variant (mean, first/last layer, step size, write norm) is a weaker predictor than entropy and mostly redundant with it. Loops: neither signal separates looping from fresh tokens (AUC below 0.5 for both). Negative result; the token-level files are regenerated by the script in about 215 s |
| `surprise_vs_entropy_step250` | final chat checkpoint, step 250 | 29e0f8558d157cbf | ccfa446 + the corrected-digest script edit committed next (recorded `ccfa446+dirty`; the JSON has no `device` field because of a comment bug fixed in the same commit; the run was on MPS per the pipeline's own execution record) | repeat of step 4 on the final checkpoint, same protocol, assistant tokens: entropy Spearman 0.86 / AUC 0.85; surprise 0.16 / 0.57; increment point estimates +0.000015 R² and +0.00021 AUC (bootstrap means +0.00011 / +0.00025; the R² interval stays above zero, the AUC interval crosses it); loop AUC entropy 0.42 / 0.39, surprise 0.50 / 0.38 at T 0.3 / 0.7 (score direction, not a joint test). For these runs: tiny increment, no surprise-driven sampler |
| `final_step250_seed0_exclude` | final chat checkpoint, step 250 | 29e0f8558d157cbf | ccfa446 + uncommitted edits at launch (recorded `ccfa446+dirty`; the working tree then held the surprise-script and status-doc edits committed as 509dd2d, no experiment-code change) | 24 taught facts + 2 boundary + 4 planted contradictions, seed 0, W0 × 20 steps, lr 1e-4, replay ratio 0.5, every arm, product rule `flagged_policy=exclude`. The teach session is uncalibrated observational: 29 of 30 turns flagged, so the gated arms' selected text was ONE turn (anchor/distill/dream still loaded the full session state). All-facts ceiling 3/24 (p 2/24). Every sleep arm 0/24 taught (ungated p 1/24 is a containment artifact: 'the birds and the bees' for expected 'bees'); anchor reproduced one planted contradiction verbatim ('water boils at 50 degrees Celsius') that the floor did not; dream rejected by cluster share 0.31. `sleep_controls.md` is the launch-time table with the poison/general attribution collision; `sleep_controls_recounted.md` recounts every arm with the fixed attribution (ASTRA-195) and states the selected-turn counts. 2.8 h wall |
| `final_step250_seed0_ceiling_single` | final chat checkpoint, step 250 | 29e0f8558d157cbf | 777075e (recorded) | single-fact ceiling (`--ceiling-mode single`), seed 0, same 24 taught + 2 boundary probes, teaching temperature 0.7: only the probed fact is taught before each probe. Taught 22/24 verbatim, 18/24 unseen phrasing; boundary 1/2 (p 1/2). Against 3/24 (p 2/24) when all 30 statements are taught first: the same checkpoint uses one taught fact in context 22 times out of 24, and loses it under the 30-statement session |
| `final_step250_seed0_include` | final chat checkpoint, step 250 | 29e0f8558d157cbf | b926e4d (recorded; the arms loaded the code of that checkout) | seed 0 again, `--flagged-policy include` so every harness-accepted turn is selected (30 of 30; the table states selected/accepted/flagged/excluded from the recorded selection), arms replay, distill, dream (anchor omitted: identical state to the exclude run). Replay accepted 0/24, NLL 1.612 → 1.485; distill accepted 0/24, → 1.474; dream: 180 generated from the 30-turn teacher state, 178 duplicates of one acknowledgement, 2 kept, rejected by cluster share 0.31 > 0.25, → 1.460. Attribution fixed at launch, so `sleep_controls.md` needs no recount. Sessions archived |
| `final_step250_seed1_include` | final chat checkpoint, step 250 | 29e0f8558d157cbf | 469211f (recorded) | seed 1, same protocol as seed 0 with `--flagged-policy include`, no all-facts ceiling; first run whose sleep reports carry `grad_norms` and `w0_relative_change`. Floor 0/24. Anchor accepted 0/24 (NLL 1.463 → 1.444); replay accepted 0/24 (→ 1.341; general verbatim 3/7 → 0/7); distill accepted 0/24 (→ 1.372); dream rejected before training: 180 generated, 180 degenerate, 179 of them 'Earth. Earth. Earth…', the echo of the last teaching turn; ungated accepted 0/24 (→ 1.344; general verbatim 0/7; one unseen-phrasing containment hit: 'Name the season I prefer.' expected 'autumn', answered by listing all four seasons, so a containment artifact). The teaching session itself collapsed into word-echo loops ('Cello. Cello. Cello…'). Sessions archived |
| `final_step250_seed2_include` | final chat checkpoint, step 250 | 29e0f8558d157cbf | b2d902d (recorded) | seed 2, same protocol as seed 1. Floor 0/24. Every arm accepted, every arm 0/24 taught: anchor (NLL 1.637 → 1.617; reproduced 'water boils at 50 degrees Celsius' as seed 0 did), replay (→ 1.492), distill (→ 1.493), dream (→ 1.499), ungated (→ 1.499). Dream: 180 generated, 144 duplicates, 24 kept of which 22 were 'The Moon is larger than the Earth', the last-taught planted contradiction, whatever fact the prompt quoted; after training the child answers the verbatim Moon question with exactly that sentence (count unchanged at 1/4, content changed); the unseen phrasing did not flip. Sessions archived |
| `anchor_transfer_analysis_step250.json` | seeds 0, 1, 2 above | 29e0f8558d157cbf | `scripts/experiments/anchor_transfer_analysis.py` on the archived folders | what the anchor fold moves: the planted turns rank 15–29, 14–29 and 8–14 of 30 by committed write on seeds 0, 1 and 2 (the first turn writes most in every seed); the fold lifts every short factual answer's stored likelihood (planted +1.9/+2.6/+2.3, general +1.2/+2.0/+1.3 nats per token) while the taught personal facts move +0.3/+0.5/+0.3: read as a shift toward short factual completions rather than storage of the taught facts; that reading is an interpretation, since the archived likelihood and recall comparison does not separate storage from retrieval |
| `jev_grade_step250.json` + `jev_grade_step250.md` | the five final-checkpoint runs above | 29e0f8558d157cbf | `plastic/sleep/grade.py` (typesafe.ai Jev, two yes/no judgments per reply) | semantic recount of all 1,664 archived replies beside the containment score: both graders agree on 0/24 taught facts in every sleep arm; 66 containment hits are artifacts by Jev's reading (wrong-unit '100°F', garbled Earth/Moon sentences, 'birds and the bees', topic-only uses of the taught word); the single-fact ceiling is 16/26 verbatim and 13/26 unseen phrasing under Jev against 23/26 and 19/26 by containment; Jev's own 18 extra hits are mostly bare affirmations and are not added to any count. Reply text was sent to the typesafe.ai API |

**Code column.** Runs launched before 2026-09-23 15:40 UTC did not record their execution commit; the
values marked ≈ are the latest commit of the relevant code at launch time as reconstructed from the
session log and may be off by a documentation-only commit. From this point every `sleep_controls.json`
and `sleep_report.json` carries `code_commit` (short SHA, `+dirty` if the tree had changes) captured at
launch. The standalone floor arm now also records the expected-answer log-probability; in earlier
runs it is present only in each sleep report's `before` block, which measures the same parent from
the same fresh state.

The intermediate SFT checkpoints are training artifacts of job `6ab351f352d0dbd7f1d82429` and are not
published; the released chat checkpoint will be pinned in `deploy/huggingface/pretrained.py`
(`TTT_CHAT`) and all later runs use it. Readings of every table are in
`docs/research/2026-09-23-sleep-consolidation.md`; the community evidence that motivated the study set
and the dream method is in `docs/research/2026-09-23-sleep-community-research.md`.

Collapse artifacts: a "1/6 taught" cell whose report shows a large `max_cluster_share` is one sentence
containing a name, not recall. Read recall together with `max_cluster_share` and `mean_answer_logprob`.
