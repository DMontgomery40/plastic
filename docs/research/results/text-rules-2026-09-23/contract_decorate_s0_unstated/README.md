# Text rule contract report

Code 9ae91dd+dirty, checkpoint digest `29e0f8558d15`, device mps, rule set `decorate`, seed 0, 17 training compositions, 8 held-out pairs, 8 situations per episode, stream 17 episodes, lasting update target w0, lr 0.0001, 20 steps, poison operator `#P` (consistent), rules UNSTATED in every preface, 8 held-out chat rows. Exact is teacher-forced (`exact_tf`). Every mode reloads the checkpoint.

| Mode | Held-out exact (adapt) before → after | of which first situation Δ / later Δ | Held-out nll (adapt) before → after | Held-out nll (no adapt) Δ | Speed area before → after | Forgetting nll Δ | Poison harm (nll) | Sequential poison: accepted, harm (nll) | Correction residual | Format-only gain exact (true) | Revert | Accepted good / refused bad | Verifier |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| frozen | 0.74 → 0.74 | +0.00 / +0.00 | 0.417 → 0.417 | +0.000 | 0.05 → 0.05 | +0.000 | +0.000 | None, +0.000 | +0.000 | +0.00 (+0.00) | ok | None / None (n 0/0) | n/a |
| continued | 0.74 → 0.88 | +0.00 / +0.15 | 0.417 → 0.155 | -0.242 | 0.05 → 0.02 | -0.294 | +0.014 | True, -0.015 | -0.009 | -0.05 (+0.13) | ok | 1.0 / 0.0 (n 2/3) | n/a |
| replay_verify | 0.74 → 0.88 | +0.00 / +0.15 | 0.417 → 0.155 | -0.242 | 0.05 → 0.02 | -0.294 | +0.014 | False, +0.000 | -0.009 | -0.05 (+0.13) | ok | 1.0 / 0.3333333333333333 (n 2/3) | v2 |

**Correction (OPUS-LEAD-001/002, 2026-09-24).** (1) Coverage: the lasting update drew 20 stream episodes with replacement from `Random(7)` (`_train_on` at this run's commit). On this 17-episode stream, whose episode *i* is training composition *i*, that trains 11 compositions: `#P`, `#P #Q`, `#Q #W`, `#Q #H`, `#H #P` and `#H #B` received no update. The poisoned stream differs from the clean one in 2 of the 20 steps (`#P #W`, `#W #P`), so the poison rows compare updates that share 18 of 20 steps. (2) Held-out repeats: `#H #Q` and `#Q #P` write the same answer as the training compositions `#Q #H` and `#P #Q` on every input (a prefix word and a suffix word commute), so two of the eight held-out pairs are not new behaviour. The code now trains in full passes, records the compositions each update trained, and names repeat pairs in every report.

**Correction to the readings of this run (OPUS-LEAD-001, 2026-09-24).** (3) The refused-bad row is unexplained. The verify set was the first six training compositions (the five singles and `#P #Q`), three situations each; `#P` and `#P #Q` were never trained, so the two first-situation hits after the clean stream were among `#Q`, `#B`, `#W` and `#H`, and the item the sequential poison removed most likely contains no `#P`. Shared W0 means the two poisoned steps could still move an unrelated item, and no clean-again control was run, so neither "the verifier caught the poison" nor "a second training pass" is shown. (4) "Rule content is stored for the trained compositions only" compared singles (the verify set) with pairs (held-out). On the contract's matched measure, first-situation exact after the update is 1/17 on the training compositions and 0/16 on the held-out pairs, while first-situation NLL falls on both (2.38 → 0.61 and 2.88 → 1.21). Exact on a handful of items cannot separate stored content from answer format; the reading is withdrawn pending the per-item first-situation choice score.
