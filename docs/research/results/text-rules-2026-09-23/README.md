# Text rule contract (T4): first measurements on the step-250 chat checkpoint

Checkpoint `ttt_mlp_760m_chat_v1` step 250 (backend digest `29e0f855…`), MPS, code 6d6f397 plus the stated-rule
preface committed next. Scoring is teacher-forced: per situation, the mean NLL over the assistant's answer tokens and
`exact_tf`, every answer token equal to the argmax given the true prefix. `adapt` means the fast weights write
during the episode; `frozen` means `freeze=True` (the slow parameters alone). Word lists of 3 to 4 nouns in the
first smoke, 4 to 5 afterwards.

| File | What | Result |
| --- | --- | --- |
| `smoke_unstated_step250.log` | Preface without the rule definitions; six singles and the 8 held-out pairs of the earlier split, 6 situations per episode; an everything-in-context probe with 8 training lessons prepended | Singles: `adapt` NLL falls from 4.9 at the first situation to about 1.0 to 1.5 from the third, `exact_tf` 2 of 36; `frozen` NLL about 8 throughout, `exact_tf` 0 of 36. Held-out pairs: `adapt` 6.5 → 1.8, `exact_tf` 1 of 48; `frozen` 0 of 48. In context: `exact_tf` 0 of 32, NLL 3.16 with 384 context situations, no better than the episode alone. Greedy from a fresh state writes chat prose, not a list |
| `gate_curve_stated_step250.log` | The in-context gate (the sources memo's first falsifier): preface states all six rules; per single operator, 3 episodes of 8 situations, `exact_tf` and NLL by situation | `exact_tf` 0.0 at every situation for #R, #S, #D; 1 of 3 at the seventh situation for #K and #T; NLL falls to about 1 nat within two or three situations for every operator (format and word copying), never to an exact list (#U reaches 0.45 nats and still 0 exact). `frozen` `exact_tf` 0. Held-out pairs (16 episodes of 8): `exact_tf` 0 at situations 1 to 3, then 0.12 from the fifth, all of it from `#D #K`, whose correct output is a single word; NLL 5.6 → 1.7. The per-operator curves are in `gate_curve_stated_step250.json` |
| `probe_simple_step250.*` | Six simpler single-output or copying operators, rules stated, 4 episodes of 8 | `exact_tf` 0 throughout for first / last / second word, count-as-a-word and alphabetical-first (NLL 1 to 5); **repeat the list exactly: 0.5 at the second situation, 1.0 from the third, NLL 0.05.** The checkpoint copies in context; it does not select or transform |
| `probe_decorations_step250.*` | Six decoration operators (fixed tokens inserted around or into a copied list) and four ordered pairs of them, rules stated, 4 episodes of 8 | Prefix "please" 1.0 from the fourth situation, suffix "thanks" 1.0 from the fourth, square brackets 1.0 from the third, doubling 0.5 to 1.0 from the fourth; commas between words and a word between every pair fail (0). Ordered pairs `#B #P`, `#P #B`, `#Q #B`, `#B #Q` (order changes the output) all reach 1.0 by the third to fifth situation when the pair itself is shown |
| `contract_decorate_s0/` | The first contract report: decoration set, seed 0, 5 singles + 12 pairs taught, 8 held-out ordered pairs, 8 situations per lesson, stream of 17 lessons, lasting-update target W0 (lr 1e-4, 20 AdamW steps), poison `#P` taught as "thanks after", 8 held-out chat rows; modes frozen, continued, in_context, replay_verify (verifier v1: held-in checks scored with the fast path frozen); code 1c89f1c + the uncommitted edits then in the tree; 7,318 s | Held-out pairs, teacher-forced exact with in-episode adaptation, before → after the lasting update: frozen 0.67 → 0.67 (the episode-only bar; no lasting change by construction); continued 0.67 → 0.87, answer NLL 0.387 → 0.120, speed ratio at the first situation 0.28 → 0.10; in_context (17 lessons read into the fast weights before each episode, 33,456 context situations measured) 0.67 → 0.55 with NLL 0.278: more fast-weight context lowers NLL and reduces exact matches; replay_verify identical to continued and accepted all three streams. In every mode the frozen-fast-path exact stays 0 (NLL 8.40 → 8.15 after continued): the slow weights never answer alone, the update makes the fast path adapt faster. Held-out chat NLL 1.629 → 1.598 after the update (no damage). Poison harm +0.019 NLL (exact 0.87 → 0.84), correction residual −0.003; revert exact in every mode. Acceptance: continued and replay_verify 1.0 accepted-good / 0.0 refused-bad (the verifier v1 checks were vacuous on exact and blind to a consistent false rule from a snapshot; see the spec) |
| `contract_decorate_s0_format_control/` | The same report with the format-only control added (the clean lessons with answers deranged across each episode's situations) and the first/later split; modes continued and replay_verify (verifier v1); eval sources as in 978dd98 (manifest says a8b12a6+dirty: the dirty files were docs and AGENTS.md); 2,363 s | Transfer rows identical to the first report (continued and replay_verify 0.67 → 0.87, NLL 0.387 → 0.120). Split of the gain: first situation +0.00 (exact 0 before and after, NLL 2.30 → 0.92), later situations +0.22; the second situation goes 0.06 → 0.94, so after the update one worked example suffices where three were needed. Format-only control: 0.67 → 0.76 exact (+0.086 against +0.195 for the true lessons), NLL −0.205 against −0.267: shuffled answers recover 44 % of the exact gain and 77 % of the likelihood gain. Verifier v1 accepted all four streams (clean, poison, corrective, format-only); its held-in exact was 0 on both sides of every decision, and the shuffled stream lowered the frozen held-in NLL the most (−0.467 against −0.213 for the clean lessons). v1 is retired; v2 (adapting held-in checks, first-situation exact, sequential poison arm) is the next row |

Reading. On this checkpoint the in-context gate sits at the floor for exact match: eight worked examples with the
rule stated do not make the model reproduce a single-operator output, while its per-token loss on those outputs
drops to about 1 nat, which is the fast path learning the answer's shape and vocabulary. This is the outcome the
sources memo predicted for a model under 1B parameters (GPT-3 sub-1B at 0% on reversed words; format and label
vocabulary learned before the rule). The contract is therefore not measurable on this checkpoint with this rule
system: any lasting update would be judged against a ceiling of zero. The probes then found the task the checkpoint
can perform in context: copying a list with fixed decorations (prefix, suffix, brackets, doubling), including ordered
pairs of decorations whose order changes the output. That is the operator set the contract runs on next; the
transforming operators stay in the archive as the measured floor.
